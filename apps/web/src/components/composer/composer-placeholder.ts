/**
 * The empty composer's hint: what each trigger character opens. A new thread
 * has no `/` commands yet, so its hint leaves that one out.
 */
export const composerPlaceholder =
  "Ask anything. Type # to mention a file, @ for a plugin, $ for a skill, / for commands";

export const startThreadPlaceholder =
  "Ask anything. Type # to mention a file, @ for a plugin, $ for a skill";
