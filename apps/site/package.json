{
  "name": "site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs",
    "preview": "vite preview",
    "deploy": "pnpm run build && pnpm dlx wrangler@4.133.0 pages deploy dist --project-name poseidon",
    "deploy:preview": "pnpm run build && pnpm dlx wrangler@4.133.0 pages deploy dist --project-name poseidon --branch \"$(git branch --show-current)\"",
    "check-types": "tsc --noEmit",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@poseidon/ui": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@poseidon/config": "workspace:*",
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "^6.0.4",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vite": "^8.1.5",
    "vitest": "catalog:"
  }
}
