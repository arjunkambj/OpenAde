{
  "name": "desktop",
  "version": "0.0.1",
  "private": true,
  "description": "Poseidon desktop shell",
  "author": "Poseidon",
  "type": "module",
  "main": "out/main/index.cjs",
  "scripts": {
    "start": "turbo run build -F web && node scripts/build.mjs && electron .",
    "dev": "node scripts/dev.mjs",
    "dev:hmr": "concurrently -k -n web,app -c cyan,magenta \"turbo run dev -F web\" \"node scripts/dev.mjs\"",
    "build": "turbo run build -F web && node scripts/package.mjs --dir",
    "build:stable": "turbo run build -F web && node scripts/package.mjs --channel stable",
    "build:canary": "turbo run build -F web && node scripts/package.mjs --channel canary",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@poseidon/contracts": "workspace:*",
    "@poseidon/shared": "workspace:*",
    "@poseidon/testkit": "workspace:*",
    "@types/node": "catalog:",
    "@types/ws": "^8.18.1",
    "concurrently": "^10.0.4",
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "ws": "^8.21.3"
  }
}
