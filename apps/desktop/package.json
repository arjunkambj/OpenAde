{
  "name": "desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "electrobun dev",
    "dev:hmr": "concurrently -k \"bun run hmr\" \"electrobun dev --watch\"",
    "hmr": "turbo run dev -F web",
    "build": "turbo run build -F web && electrobun build",
    "build:stable": "turbo run build -F web && electrobun build --env=stable",
    "build:canary": "turbo run build -F web && electrobun build --env=canary",
    "check-types": "test -d .hutch/devkit || electrobun sync && tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "concurrently": "^10.0.4",
    "electrobun": "2.0.1",
    "typescript": "catalog:"
  }
}
