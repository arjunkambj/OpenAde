/**
 * Identity of this package while it is still a stub.
 *
 * The interfaces a connector implements, plus the conformance suite it must pass.
 * Until that lands, this module exists so the package resolves, typechecks and
 * runs a test like every other workspace package.
 */
export const PACKAGE_NAME = "@OpenAde/connector-sdk";
